---
title: "Criar Teste Nexus"
api: "POST https://api-new.paineloffice.click/nexus"
description: "Cria um teste gratuito Nexus no sistema (trial: true)"
auth: "none"
---

## Endpoint

<CodeGroup>

```bash Requisição
POST /nexus
Authorization: Bearer <token>
```

</CodeGroup>

## Descrição

Cria um novo teste gratuito Nexus no sistema. Esta é a mesma rota usada para criar usuários Nexus, mas com `trial: true` para testes gratuitos.

## Parâmetros

<ParamField query="username" type="string" required>
  Nome de usuário para autenticação
</ParamField>

<ParamField query="password" type="string" required>
  Senha para autenticação
</ParamField>

<ParamField query="token" type="string" required>
  Token de autenticação
</ParamField>

<ParamField body="username" type="string" required>
  Nome de usuário Nexus
</ParamField>

<ParamField body="password" type="string" required>
  Senha do usuário
</ParamField>

<ParamField body="whatsapp" type="number">
  WhatsApp do usuário (apenas números)
</ParamField>

<ParamField body="notes" type="string">
  Observações
</ParamField>

<ParamField body="email" type="string">
  E-mail do usuário
</ParamField>

<ParamField body="months" type="string" required>
  Duração em meses
</ParamField>

<ParamField body="sale_value" type="number" required>
  Valor da venda
</ParamField>

<ParamField body="screens" type="integer" required>
  Número de telas
</ParamField>

<ParamField body="trial" type="boolean" required default="true">
  Sempre `true` para testes gratuitos (valor fixo)
</ParamField>

## Exemplo de Requisição

<CodeGroup>

```json TryIt
{
  "email": "",
  "months": "0.5",
  "notes": "",
  "password": "588497601",
  "sale_value": 0,
  "screens": 1,
  "trial": true,
  "username": "20868876qn7",
  "whatsapp": ""
}
```

```bash cURL
curl -X POST "https://api-new.paineloffice.click/nexus" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{
    "email": "",
    "months": "0.5",
    "notes": "",
    "password": "588497601",
    "sale_value": 0,
    "screens": 1,
    "trial": true,
    "username": "20868876qn7",
    "whatsapp": ""
  }'
```

</CodeGroup>

## Resposta

<ResponseField name="success" type="boolean">
  Indica se a operação foi bem-sucedida
</ResponseField>

<ResponseField name="message" type="string">
  Mensagem de confirmação
</ResponseField>

<ResponseField name="data" type="object">
  <Expandable title="properties">
    <ResponseField name="id" type="integer">
      ID do teste Nexus criado
    </ResponseField>
    <ResponseField name="username" type="string">
      Nome de usuário Nexus
    </ResponseField>
    <ResponseField name="email" type="string">
      E-mail do usuário
    </ResponseField>
    <ResponseField name="status" type="string">
      Status do teste
    </ResponseField>
    <ResponseField name="bandwidth_limit" type="integer">
      Limite de banda
    </ResponseField>
    <ResponseField name="expires_at" type="string">
      Data de expiração do teste
    </ResponseField>
    <ResponseField name="server_url" type="string">
      URL do servidor Nexus
    </ResponseField>
    <ResponseField name="access_token" type="string">
      Token de acesso
    </ResponseField>
  </Expandable>
</ResponseField>

## Exemplo de Resposta

```json
{
  "message": "Teste Nexus criado com sucesso",
  "data": {
    "id": 3001,
    "username": "teste_nexus_001",
    "email": "teste@exemplo.com",
    "status": "active",
    "bandwidth_limit": 200,
    "expires_at": "2024-02-15T10:30:00Z",
    "server_url": "https://nexus.servidor.com",
    "access_token": "nexus_token_xyz789abc123"
  }
}
```
