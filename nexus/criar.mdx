---
title: " Criar Nexus"
api: "POST https://api-new.paineloffice.click/nexus"
description: "Cria um usuário/teste Nexus"
---

## Endpoint

<CodeGroup>

```bash Requisição
POST /nexus
```

</CodeGroup>

## Descrição

Cria um novo usuário ou teste Nexus no sistema.

## Parâmetros

<ParamField body="username" type="string" required>
  Nome de usuário Nexus
</ParamField>

<ParamField body="password" type="string" required>
  Senha do usuário
</ParamField>

<ParamField body="email" type="string" required>
  E-mail do usuário
</ParamField>

<ParamField body="is_trial" type="boolean">
  Se é um teste gratuito (padrão: false)
</ParamField>

<ParamField body="trial_duration" type="integer">
  Duração do teste em horas (obrigatório se is_trial=true)
</ParamField>

<ParamField body="package_id" type="integer" required>
  ID do pacote Nexus
</ParamField>

<ParamField body="resale_id" type="integer" required>
  ID da revenda
</ParamField>

<ParamField body="bandwidth_limit" type="integer">
  Limite de banda em Mbps (padrão: 100)
</ParamField>

## Resposta

<ResponseField name="success" type="boolean">
  Indica se a operação foi bem-sucedida
</ResponseField>

<ResponseField name="message" type="string">
  Mensagem de confirmação
</ResponseField>

<ResponseField name="data" type="object">
  <Expandable title="properties">
    <ResponseField name="id" type="integer">
      ID do usuário Nexus criado
    </ResponseField>
    <ResponseField name="username" type="string">
      Nome de usuário Nexus
    </ResponseField>
    <ResponseField name="email" type="string">
      E-mail do usuário
    </ResponseField>
    <ResponseField name="status" type="string">
      Status do usuário
    </ResponseField>
    <ResponseField name="bandwidth_limit" type="integer">
      Limite de banda
    </ResponseField>
    <ResponseField name="expires_at" type="string">
      Data de expiração
    </ResponseField>
    <ResponseField name="server_url" type="string">
      URL do servidor Nexus
    </ResponseField>
    <ResponseField name="access_token" type="string">
      Token de acesso
    </ResponseField>
  </Expandable>
</ResponseField>

## Exemplo de Resposta

```json
{
  "success": true,
  "message": "Usuário Nexus criado com sucesso",
  "data": {
    "id": 3001,
    "username": "usuario_nexus_001",
    "email": "usuario@exemplo.com",
    "status": "active",
    "bandwidth_limit": 200,
    "expires_at": "2024-02-15T10:30:00Z",
    "server_url": "https://nexus.servidor.com",
    "access_token": "nexus_token_xyz789abc123"
  }
}
```