---
title: " Renovar IPTV"
api: "PUT https://api-new.paineloffice.click/iptv/extend/{id}"
description: "Renova um usuário IPTV"
auth: "none"
---

## Endpoints

### Renovar IPTV

<CodeGroup>

```bash Requisição
PUT /iptv/extend/{id}
Authorization: Bearer <token>
```

</CodeGroup>

### Criar Teste IPTV

<CodeGroup>

```bash Requisição
POST /iptv/teste
Authorization: Bearer <token>
```

</CodeGroup>

## Descrição

Renova um usuário IPTV existente, estendendo sua validade, ou cria um teste IPTV gratuito.

## Parâmetros - Renovar IPTV

<ParamField query="username" type="string" required>
  Nome de usuário para autenticação
</ParamField>

<ParamField query="password" type="string" required>
  Senha para autenticação
</ParamField>

<ParamField query="token" type="string" required>
  Token de autenticação
</ParamField>

## Parâmetros do Body - Renovar IPTV

<ParamField body="amount" type="number" required>
  Valor da renovação
</ParamField>

<ParamField body="month" type="number" required>
  Duração em meses
</ParamField>

<ParamField body="screen" type="integer" required>
  Número de telas
</ParamField>

## Parâmetros - Criar Teste IPTV

<ParamField query="username" type="string" required>
  Nome de usuário para autenticação
</ParamField>

<ParamField query="password" type="string" required>
  Senha para autenticação
</ParamField>

<ParamField query="token" type="string" required>
  Token de autenticação
</ParamField>

<ParamField body="userline" type="string" required>
  Nome de usuário IPTV
</ParamField>

<ParamField body="passline" type="string" required>
  Senha do usuário
</ParamField>

<ParamField body="screen" type="integer">
  Número máximo de conexões simultâneas (padrão: 1)
</ParamField>

<ParamField body="packageId" type="string" required>
  ID do pacote IPTV
</ParamField>

<ParamField body="contact" type="string" required>
  Informações de contato
</ParamField>

<ParamField body="whatsapp" type="number">
  Número do WhatsApp (apenas números)
</ParamField>

<ParamField body="notes" type="string">
  Observações adicionais
</ParamField>

## Resposta

<ResponseField name="success" type="boolean">
  Indica se a operação foi bem-sucedida
</ResponseField>

<ResponseField name="message" type="string">
  Mensagem de confirmação
</ResponseField>

<ResponseField name="data" type="object">
  <Expandable title="properties">
    <ResponseField name="id" type="integer">
      ID do usuário IPTV
    </ResponseField>
    <ResponseField name="username" type="string">
      Nome de usuário
    </ResponseField>
    <ResponseField name="previous_expires_at" type="string">
      Data de expiração anterior
    </ResponseField>
    <ResponseField name="new_expires_at" type="string">
      Nova data de expiração
    </ResponseField>
    <ResponseField name="days_extended" type="integer">
      Dias estendidos
    </ResponseField>
  </Expandable>
</ResponseField>

## Exemplo de Requisição

<CodeGroup>

```json TryIt
{
  "amount": 150.01,
  "month": 1,
  "screen": 1
}
```

```bash cURL
curl -X PUT "https://api-new.paineloffice.click/iptv/extend/1001" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{
    "amount": 150.01,
    "month": 1,
    "screen": 1
  }'
```

</CodeGroup>

## Exemplo de Resposta

```json
{
  "message": "Usuário IPTV renovado com sucesso",
  "data": {
    "id": 1001,
    "username": "usuario_iptv_001",
    "previous_expires_at": "2024-02-15T10:30:00Z",
    "new_expires_at": "2024-03-16T10:30:00Z",
    "days_extended": 30
  }
}
```