---
title: " Criar Teste IPTV"
api: "POST https://api-new.paineloffice.click/iptv/teste"
description: "Cria um teste IPTV gratuito"
auth: "none"
---

## Endpoint

<CodeGroup>

```bash Requisição
POST /iptv/teste
Authorization: Bearer <token>
```

</CodeGroup>

## Descrição

Cria um novo teste IPTV gratuito no sistema.

## Parâmetros

<ParamField query="username" type="string" required>
  Nome de usuário para autenticação
</ParamField>

<ParamField query="password" type="string" required>
  Senha para autenticação
</ParamField>

<ParamField query="token" type="string" required>
  Token de autenticação
</ParamField>

<ParamField body="userline" type="string" required>
  Nome de usuário IPTV
</ParamField>

<ParamField body="passline" type="string" required>
  Senha do usuário
</ParamField>

<ParamField body="screen" type="integer">
  Número máximo de conexões simultâneas (padrão: 1)
</ParamField>

<ParamField body="packageId" type="string" required>
  ID do pacote IPTV
</ParamField>

<ParamField body="contact" type="string" required>
  Informações de e-email
</ParamField>

<ParamField body="whatsapp" type="number">
  Número do WhatsApp (apenas números)
</ParamField>

<ParamField body="notes" type="string">
  Observações adicionais
</ParamField>

## Resposta

<ResponseField name="success" type="boolean">
  Indica se a operação foi bem-sucedida
</ResponseField>

<ResponseField name="message" type="string">
  Mensagem de confirmação
</ResponseField>

<ResponseField name="data" type="object">
  <Expandable title="properties">
    <ResponseField name="id" type="integer">
      ID do teste IPTV criado
    </ResponseField>
    <ResponseField name="username" type="string">
      Nome de usuário IPTV
    </ResponseField>
    <ResponseField name="max_connections" type="integer">
      Número máximo de conexões
    </ResponseField>
    <ResponseField name="status" type="string">
      Status do teste
    </ResponseField>
    <ResponseField name="expires_at" type="string">
      Data de expiração do teste
    </ResponseField>
    <ResponseField name="connection_url" type="string">
      URL de conexão IPTV
    </ResponseField>
    <ResponseField name="m3u_url" type="string">
      URL do arquivo M3U
    </ResponseField>
  </Expandable>
</ResponseField>

## Exemplo de Requisição

<CodeGroup>

```json TryIt
{
  "contact": "1111111111111111111",
  "notes": "Teste gratuito",
  "packageId": "24",
  "passline": "70774455",
  "screen": 1,
  "userline": "229063642",
  "whatsapp": 5511111111111
}
```

```bash cURL
curl -X POST "https://api-new.paineloffice.click/iptv/teste" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{
    "contact": "1111111111111111111",
    "notes": "Teste gratuito",
    "packageId": "24",
    "passline": "70774455",
    "screen": 1,
    "userline": "229063642",
    "whatsapp": 5511111111111
  }'
```

</CodeGroup>

## Exemplo de Resposta

```json
{
  "success": true,
  "message": "Teste IPTV criado com sucesso",
  "data": {
    "id": 1002,
    "username": "teste_iptv_001",
    "max_connections": 1,
    "status": "trial",
    "expires_at": "2024-01-20T10:30:00Z",
    "connection_url": "http://servidor.iptv.com:8080",
    "m3u_url": "http://servidor.iptv.com:8080/get.php?username=teste_iptv_001&password=70774455&type=m3u_plus"
  }
}
```